{
  "name": "radar-ri",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Radar RI - painel CASH3 (Meliuz) e assistente de IA para relacoes com investidores",
  "scripts": {
    "sync": "node --env-file-if-exists=.env scripts/sync-market-data.mjs",
    "sync-news": "node --env-file-if-exists=.env scripts/sync-news.mjs",
    "ingest": "node --env-file-if-exists=.env scripts/ingest-ri-documents.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4"
  },
  "engines": {
    "node": ">=22"
  }
}
